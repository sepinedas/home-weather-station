dependencies:
  lvgl/lvgl:
    version: "9.2.2"
  # crowpanel_epaper_driver_component is NOT a managed dependency here;
  # it's vendored (patched) under components/crowpanel_epaper_driver_component
  # because the published package fails to build on this project's
  # ESP-IDF version. See that directory's README.md for details.
